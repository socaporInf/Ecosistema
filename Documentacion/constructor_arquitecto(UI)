Arquitecto: {
  elementos: array, ----- arreglo que contiene los elementos de la interfaz
  estado: string,

  Metodos
    configure() ------------------------- funcion de arranque del arquitecto crea menu y cabecera
    crearVentanaModal(data) ------------- crea una ventana modal con los datos pasados
    crearMensaje(tipo,mensaje) ---------- crea una ventana modal sencilla del tipo mensaje con los datos pasado como parametros
    crearCuadroDeCarga(info,contenedor) - se utiliza cuando se necesita pasar parametros al callback al culminar la carga
    iniciarCarga(info,callback) --------- se utiliza cuando no se necesita pasar parametros al callback al culminar la carga

}
