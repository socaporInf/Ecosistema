cuadroCarga(info,callback): {
  contenedor: DOM, ----- parte de la interfaz donde se mostrara el gif de cuadroCarga
  tipo: string, -------- trae por defecto el valor de carga, por ahora en desuso
  nodo: DOM,
  intervalID: numeric,
  contEspera; numeric, - conta dor con el cual se lleva control del tiempo de espera de la peticion
  callaback: function, - funcion que se ejecutara al finalizar la carga
  nombre: string, ------ permite la ubicacion del objeto dentro del arreglo global de cuadros en UI.cuadroCarga
  clases: Array, ------- guarda todas las clases asignadas
  estado: string,

  metodos:
    construirNodo()
    manejarCarga(nombre) -------------- crea un intervalo de carga que permite manejar dicha carga colocandole un tiempo de espera 5 segundos
    culminarCarga(respuesta,callback) - funcion en la cual se le pasa parametros al callback al culminar la carga y muestra un mensaje al culminar la carga
    terminarCarga() ------------------- mata el intervalo al ejecultarce el callback de dicha carga
    manejoDeClases() ------------------ asigna todas las clases pasadas como parametro al html y las guarda en un arreglo en this.clases
    eliminarClasesRepetidas() --------- elimina las clases repetidas del arreglo this.clases

}
este objeto recibe como parametro de creacio un parametro con esta estructura
{
  nombre: 'guardarEdicion',
  mensaje:'Guardando cambios',
  contenedor:contenedor,
};
